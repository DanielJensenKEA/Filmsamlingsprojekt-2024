/*
import java.util.Scanner;

public class KodeNoterProjekt {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        //Mere brugervenlig input i fht. boolean kode
        boolean isInColor = false;
        System.out.println("Er filmen i farve, skriv ja, ellers skriv nej?");
        //Vi bruger ikke nextLine(), da vi kun vil have et ord uden mellemrum fra brugeren
        String erIFarve = scanner.next();
        //Vi gør brugerinputtet erIFarve til lowercase nedenfor.
        erIFarve = erIFarve.toLowerCase();
        if(erIFarve.equals("ja")) {
            isInColor = true;
        }

        //toString metode gennemgang
        @Override i Movie klasse. "Stavekontrol for Java"
        toString i Movie klassen
        public String toString() {
            result = "";
            result += title + " " + genre + " " + ...;
            if(erIFarve=true) {
                result += " er i farve";
            }
            else(erIFarve=false) {
                result += " er ikke i farve"
            }
            return result;
        }





    }
}

 */
